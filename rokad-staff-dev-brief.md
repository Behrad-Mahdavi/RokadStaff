# بریف فنی ماژول گزارش‌گیری / گزارش‌دهی — Rokad Staff

> این سند مکمل `rokad-staff-dev-brief.md` است و فقط ماژول آنالیتیکس/گزارش‌گیری ادمین‌پنل را پوشش می‌دهد (بخش‌های ۴ و ۱۱ فاز ۲ از سند اصلی، اینجا با جزئیات کامل باز شده‌اند). دیزاین در `design.md` جداست.

---

## ۱. تفکیک مفهومی — قبل از شروع مهم است

دو مفهوم متفاوت‌اند که نباید قاطی شوند:

- **گزارش‌دهی (Reporting به معنای ورودی)**: همان چیزی که در سند قبلی پوشش داده شد — کارمند از طریق تلگرام گزارش کار می‌فرستد. **این ماژول به آن کاری ندارد و فقط مصرف‌کننده‌ی آن داده است.**
- **گزارش‌گیری (این ماژول)**: تبدیل داده‌ی خام ثبت‌شده (`daily_reports` + `report_items`) به بینش قابل استفاده برای مدیر — آمار، روند، فیلتر، و خروجی فایل.

این سند فقط لایه‌ی دوم را طراحی می‌کند: **چطور داده‌ی موجود را می‌خوانیم، تجمیع می‌کنیم، فیلتر می‌کنیم و خروجی می‌دهیم** — بدون اینکه به Schema ورودی دست بزنیم.

---

## ۲. Scope این ماژول

### داخل Scope
1. داشبورد آماری کلی (KPI Cards)
2. لیست/جدول گزارش‌ها با فیلتر پیشرفته
3. گزارش "کارمندان بدون ثبت" (`missing reports`)
4. روند زمانی (هفتگی/ماهانه) نرخ تکمیل
5. خروجی Excel از هر View فیلترشده
6. کنترل دسترسی مبتنی بر نقش (Admin کل‌بین، Supervisor محدود به دپارتمان خودش)

### خارج از Scope (این نسخه)
- گزارش‌های پیش‌بینی‌کننده (Predictive Analytics)
- اعلان Real-time (WebSocket) — فعلاً Polling/Refresh دستی کافی است
- خروجی PDF (فقط Excel در این فاز)

---

## ۳. تعریف دقیق متریک‌ها

هر متریک باید یک تعریف ریاضی دقیق داشته باشد وگرنه بین توسعه‌دهنده و مدیر برداشت متفاوت پیش می‌آید:

| متریک | فرمول | توضیح |
|---|---|---|
| **Completion Rate (نرخ ثبت گزارش)** | `تعداد کارمندان ثبت‌کرده در بازه / تعداد کل کارمندان فعال در بازه × ۱۰۰` | بر اساس روزهای کاری محاسبه شود، نه تقویمی (باید تعریف "روز کاری" مشخص شود - بخش ۸) |
| **On-Time Rate (نرخ به‌موقع بودن)** | `تعداد گزارش‌های status=on_time / کل گزارش‌های ثبت‌شده × ۱۰۰` | مستقل از Completion Rate است؛ یک کارمند می‌تواند همیشه ثبت کند ولی همیشه دیر |
| **Task Completion Ratio (نرخ تکمیل تسک‌ها)** | `تعداد report_items با status=done / کل report_items ثبت‌شده در بازه × ۱۰۰` | این متریک سطح "تسک" است، نه سطح "گزارش" |
| **Missing Reports (تعداد غیبت گزارش)** | `کارمندان فعال − کارمندان ثبت‌کرده در یک روز مشخص` | برای هر روز جداگانه محاسبه می‌شود، نه میانگین بازه |
| **Streak (روزهای متوالی ثبت)** | تعداد روزهای کاری متوالی که یک کارمند بدون وقفه گزارش داده | فاز ۲.۵ — اگر مدیر بخواهد به‌عنوان انگیزه‌ی داخلی استفاده کند |

> نکته‌ی مهم Counter-Metric: اگر فقط روی **Completion Rate** تمرکز کنی، ممکن است کارمندان برای بالا نگه‌داشتن آمار، گزارش‌های سطحی و کوتاه (مثلاً یک خط بی‌معنی) بفرستند صرفاً برای رد شدن از فیلتر. **Task Completion Ratio** و طول متوسط گزارش باید به‌عنوان Guardrail Metric کنار Completion Rate دیده شوند، نه به‌تنهایی.

---

## ۴. معماری داده‌ی این ماژول

### مشکل: کوئری زنده (Real-time Aggregation) روی داده‌ی خام کافی نیست
اگر تعداد کارمندان و بازه‌ی زمانی زیاد شود (مثلاً گزارش سالانه‌ی ۲۰۰ کارمند)، اجرای `JOIN` و `GROUP BY` زنده روی `daily_reports` + `report_items` برای هر بار باز شدن داشبورد کند و پرهزینه می‌شود، مخصوصاً روی Neon Serverless که Connection ها Cold Start دارند.

### راه‌حل: جدول تجمیع‌شده‌ی روزانه (Pre-Aggregation)

```sql
CREATE TABLE daily_stats (
    id                  UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    stat_date           DATE NOT NULL,
    department          TEXT,                 -- NULL یعنی کل سازمان
    active_employees    INT NOT NULL,
    submitted_count     INT NOT NULL,
    on_time_count       INT NOT NULL,
    late_count          INT NOT NULL,
    total_task_items    INT NOT NULL,
    done_task_items     INT NOT NULL,
    computed_at         TIMESTAMPTZ NOT NULL DEFAULT now(),

    UNIQUE(stat_date, department)
);
```

**چطور پر می‌شود؟** یک Vercel Cron شبانه (مثلاً ساعت ۰۲:۰۰ به وقت تهران، بعد از پایان قطعی همه‌ی گزارش‌های آن روز) یک‌بار روی `daily_reports` و `report_items` مربوط به همان روز، Aggregation انجام می‌دهد و نتیجه را در `daily_stats` می‌نویسد (یک ردیف برای کل سازمان + یک ردیف به‌ازای هر دپارتمان).

**چرا این معماری بهتر است؟**
- داشبورد فقط از `daily_stats` می‌خواند → کوئری‌های سبک و سریع، حتی برای بازه‌ی یک سال
- محاسبه‌ی سنگین فقط یک‌بار در شبانه‌روز اجرا می‌شود، نه در هر Page Load
- اگر روزی منطق محاسبه‌ی متریک تغییر کند، فقط Job محاسبه را دوباره روی داده‌ی تاریخی اجرا می‌کنی (Backfill) بدون نیاز به تغییر در لایه‌ی UI

**Trade-off صریح:** داده‌ی داشبورد "امروز" تا قبل از اجرای Cron شبانه، دقیق نیست (چون هنوز Aggregate نشده). اگر مدیر نیاز به دیدن آمار **لحظه‌ای همان روز** دارد (مثلاً ساعت ۱۵:۰۰ ببیند چند نفر تا الان گزارش داده‌اند)، باید یک مسیر جدا برای "امروز" داشته باشیم که مستقیم از داده‌ی خام (Live Query بدون Cache) بخواند، چون آن حجم داده (فقط یک روز) سبک است و مشکلی ایجاد نمی‌کند. بازه‌های تاریخی (دیروز به عقب) همیشه از `daily_stats` خوانده شوند.

---

## ۵. API Endpoints این ماژول

| Method | Route | توضیح |
|---|---|---|
| `GET` | `/api/reporting/dashboard?from=&to=&department=` | KPI Cards خلاصه (از `daily_stats` می‌خواند، به‌جز بازه‌ای که شامل امروز باشد) |
| `GET` | `/api/reporting/today-live` | آمار لحظه‌ای امروز (Live Query مستقیم روی داده‌ی خام) |
| `GET` | `/api/reporting/trend?metric=&from=&to=&groupBy=day|week|month` | داده‌ی سری‌زمانی برای نمودار روند |
| `GET` | `/api/reporting/missing?date=&department=` | لیست کارمندانی که در تاریخ مشخص ثبت نکرده‌اند (Live Query - این جدول کوچک است) |
| `GET` | `/api/reporting/employee/:id/history?from=&to=` | تاریخچه‌ی کامل گزارش‌های یک کارمند خاص |
| `GET` | `/api/reporting/export?from=&to=&department=&format=xlsx` | تولید و دانلود فایل Excel از داده‌ی فیلترشده |
| `POST` | `/api/cron/aggregate-daily-stats` | فراخوانی‌شده توسط Vercel Cron شبانه برای پر کردن `daily_stats` |

همه‌ی این Route ها باید Scope دسترسی را بر اساس نقش کاربر (بخش ۷) اعمال کنند — یعنی حتی اگر Supervisor مستقیماً پارامتر `department` دیگری در URL بفرستد، Backend باید رد کند، نه اینکه به فرانت‌اند اعتماد کند.

---

## ۶. منطق فیلترها

فیلترهای مشترک بین همه‌ی Endpoint های بالا:

- **بازه‌ی تاریخ (`from`, `to`)**: همیشه بر اساس `Asia/Tehran` تفسیر شود، نه UTC خام (همان قانونی که در سند اصلی برای `report_date` گفته شد باید اینجا هم رعایت شود).
- **دپارتمان**: می‌تواند تک‌مقداری یا چندمقداری (`department[]`) باشد.
- **کارمند خاص**: برای Drill-down از لیست کلی به یک نفر.
- **وضعیت**: `on_time` / `late` / `missing`.

### قانون مهم: کارمندان غیرفعال‌شده در وسط بازه
اگر کارمندی در وسط بازه‌ی گزارش‌گیری `is_active = false` شده باشد، نباید از مخرج کسر `Completion Rate` کنار گذاشته شود برای روزهایی که هنوز فعال بوده. یعنی محاسبه‌ی "تعداد کارمندان فعال" باید **per-day** باشد نه بر اساس وضعیت فعلی کارمند. این دقیقاً دلیلی است که ستون `active_employees` در `daily_stats` per-day ذخیره می‌شود، نه محاسبه‌شده در لحظه‌ی نمایش.

---

## ۷. کنترل دسترسی (Access Scope)

| نقش | دسترسی |
|---|---|
| `admin` | تمام دپارتمان‌ها، تمام کارمندان، Export نامحدود |
| `supervisor` (فاز ۲) | فقط دپارتمان(های) تخصیص‌یافته به خودش؛ نمی‌تواند گزارش کارمند دپارتمان دیگر را حتی با تغییر URL ببیند |

پیاده‌سازی: یک Middleware مشترک در تمام Route های `/api/reporting/*` که نقش کاربر را از Session می‌خواند و در صورت Supervisor بودن، پارامتر `department` درخواستی را با لیست دپارتمان‌های مجاز او Intersect می‌کند (نه صرفاً اعتماد به ورودی).

---

## ۸. سؤالات باز مخصوص این ماژول

1. **تعریف "روز کاری"**: آیا پنجشنبه/جمعه از محاسبه‌ی Completion Rate حذف می‌شوند؟ این مستقیم روی فرمول بخش ۳ اثر دارد و باید در جدول تنظیمات سازمان قابل تعریف باشد (چون ممکن است شرکت‌های مختلف تعطیلات متفاوت داشته باشند).
2. **دوره‌ی نگه‌داری داده‌ی خام**: آیا `daily_reports` و `report_items` قدیمی‌تر از مثلاً یک سال باید Archive/حذف شوند، یا برای همیشه نگه داشته می‌شوند؟ این روی نیاز به Partitioning جدول در آینده اثر دارد.
3. **دسترسی Export**: آیا Supervisor هم مجاز به خروجی Excel است یا این قابلیت فقط برای Admin است؟
4. **تعطیلات رسمی**: آیا روزهای تعطیل رسمی (نه فقط آخر هفته) باید از محاسبه‌ی Completion Rate کنار گذاشته شوند؟ اگر بله، نیاز به یک جدول `holidays` جداگانه است که باید در این فاز یا فاز بعد اضافه شود.

---

## ۹. خروجی Excel — جزئیات فنی

- کتابخانه‌ی پیشنهادی: `exceljs` (روی Vercel Serverless سازگار است، برخلاف برخی کتابخانه‌های مبتنی بر Binary سنگین).
- فایل باید در حافظه (Buffer) تولید و مستقیم به‌عنوان Response با `Content-Type: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet` برگردانده شود — نیازی به ذخیره‌ی فایل روی دیسک یا Storage خارجی نیست (Vercel Function Filesystem موقتی و غیرقابل‌اتکا برای این کار است).
- محدودیت زمان اجرای Vercel Function (بسته به Plan، معمولاً ۱۰-۶۰ ثانیه) را در نظر بگیر — اگر بازه‌ی Export خیلی بزرگ باشد (مثلاً کل یک سال، همه‌ی دپارتمان‌ها)، باید یا محدودیت روی حداکثر بازه‌ی مجاز Export گذاشته شود، یا فرآیند به یک Background Job (مثلاً با یک صف ساده) منتقل شود. برای فاز ۱ این ماژول، محدودیت ساده روی بازه (مثلاً حداکثر ۹۰ روز در هر Export) پیشنهاد می‌شود.

---

## ۱۰. تست و اعتبارسنجی (QA Checklist)

- [ ] محاسبه‌ی Completion Rate با یک کارمند که در وسط ماه غیرفعال شده، تست شود (بخش ۶)
- [ ] Timezone Edge Case: گزارشی که ساعت ۲۳:۵۰ به وقت تهران ثبت شده، در `daily_stats` روز درست حساب شود، نه روز بعد به وقت UTC
- [ ] Aggregation Cron باید Idempotent باشد — اگر به هر دلیل دوبار برای یک روز اجرا شود، نباید داده تکراری/دوبرابر تولید کند (`UPSERT` بر اساس `UNIQUE(stat_date, department)`)
- [ ] Supervisor نتواند با تغییر Query Param به داده‌ی دپارتمان دیگر دسترسی پیدا کند (تست امنیتی، نه فقط UI)
- [ ] Export با بازه‌ی خالی (بدون هیچ گزارشی) باید فایل معتبر با پیام "داده‌ای یافت نشد" برگرداند، نه خطا یا فایل خراب

---

این ماژول کاملاً روی داده‌ی موجود در `rokad-staff-dev-brief.md` سوار می‌شود؛ هیچ تغییری در جداول اصلی (`employees`, `daily_reports`, `report_items`) لازم نیست — فقط دو جدول جدید (`daily_stats` و در آینده `holidays`) اضافه می‌شود.