# بریف فنی تکمیلی — تسک‌های فردی / دکمه تسک سریع / پنل گزارش‌گیری یکپارچه

> این سند مکمل سه سند قبلی است (`rokad-staff-dev-brief.md`, `rokad-staff-reporting-module-brief.md`, `rotello-staff-module-brief.md`) و سه ویژگی جدید را روی همان زیرساخت اضافه می‌کند. دیزاین در `design.md` جداست.

---

## ۰. نقشه‌ی کلی این سند — چه چیزی به چه چیزی اضافه می‌شود

| # | ویژگی | جایگاه در سیستم |
|---|---|---|
| ۱ | **تسک‌های فردی (Individual Tasks)** | توسعه‌ی جدول `tasks` موجود در Rotello Staff — تسک بدون پروژه |
| ۲ | **دکمه‌ی تسک جدید (Quick Add) در صفحه‌ی اول داشبورد** | یک مسیر میان‌بر برای ساخت تسک پروژه‌ای بدون باز کردن بورد |
| ۳ | **پنل گزارش‌گیری فردی یکپارچه** | یک لایه‌ی جدید که داده‌ی Rokad Staff (گزارش روزانه) و Rotello Staff (تسک‌ها) را برای یک نفر کنار هم می‌آورد |

نکته‌ی مهم قبل از شروع: هر سه ویژگی روی جداول موجود سوار می‌شوند؛ هیچ‌کدام نیاز به بازطراحی جداول قبلی ندارند، فقط توسعه (Extend) می‌دهند.

---

## ۱. تسک‌های فردی (Individual Tasks)

### تعریف دقیق
تسک فردی یعنی یک تسک که **به هیچ پروژه/بوردی تعلق ندارد** — مستقیماً توسط مدیر به یک (یا چند) کارمند اختصاص داده می‌شود، بدون نیاز به عبور از یک بورد کانبان. مثال: "گزارش هزینه‌های سفر ماه قبل رو تا فردا آماده کن" — این یک کار مستقل است، نه بخشی از یک پروژه.

### تصمیم معماری: توسعه‌ی جدول `tasks` به‌جای ساخت جدول جدید
به‌جای ساخت یک جدول موازی (`individual_tasks`)، جدول `tasks` موجود توسعه داده می‌شود تا `project_id` قابل `NULL` بودن باشد. دلیل این تصمیم:
- تسک فردی همچنان به همان زیرساخت‌های آماده نیاز دارد: چندین Assignee (`task_assignees`)، چک‌لیست‌های چندگانه (`checklists`)، تایم‌لاین گزارش عملکرد Append-only (`task_reports`)، Activity Log. ساخت جدول جدا یعنی تکرار کامل این چهار جدول و منطق‌شان.
- "میز کار شخصی" (بخش ۱۱ سند Rotello) از قبل بر مبنای `task_assignees` کار می‌کند، نه بر مبنای پروژه — پس با یک تغییر کوچک در Query (حذف فیلتر اجباری پروژه)، تسک‌های فردی هم به‌طور طبیعی در میز کار شخصی ظاهر می‌شوند.

### تغییرات Schema

```sql
-- ۱. project_id و column_id قابل NULL می‌شوند
ALTER TABLE tasks ALTER COLUMN project_id DROP NOT NULL;
ALTER TABLE tasks ALTER COLUMN column_id DROP NOT NULL;

-- ۲. فیلد وضعیت مستقل برای تسک‌های فردی (چون این تسک‌ها ستون کانبان ندارند تا وضعیت از آن استخراج شود)
ALTER TABLE tasks ADD COLUMN status TEXT; -- 'todo' | 'in_progress' | 'done' | 'cancelled'

-- ۳. قانون یکپارچگی داده: یا تسک پروژه‌ای کامل است، یا تسک فردی کامل است — حالت نصفه‌نیمه مجاز نیست
ALTER TABLE tasks ADD CONSTRAINT chk_task_type CHECK (
    (project_id IS NOT NULL AND column_id IS NOT NULL AND status IS NULL)
    OR
    (project_id IS NULL AND column_id IS NULL AND status IS NOT NULL)
);
```

> این `CHECK Constraint` عمداً سخت‌گیرانه است تا از حالت ناخواسته‌ی "تسک فردی که project_id هم دارد" یا برعکس، در سطح دیتابیس جلوگیری شود — نه فقط در Application Layer که قابل دور زدن با باگ است.

### چه کسی می‌تواند تسک فردی بسازد/ویرایش/حذف کند؟
چون تسک فردی به هیچ پروژه‌ای تعلق ندارد، نقش "Project Manager" (per-project) اینجا بی‌معنی است. قانون پیشنهادی:
- **ساخت**: فقط Owner/مدیر سراسری (همان کسی که در سند اصلی نقش Admin دارد) — بر خلاف تسک پروژه‌ای که همه‌ی اعضا مجاز به ساخت بودند (تصمیم قبلی)، اینجا چون هدف "واگذاری مستقیم کار از بالا" است، منطقی است محدودتر باشد.
- **ویرایش/حذف**: فقط همان Owner/مدیر سراسری که آن را ساخته یا هر Owner دیگر.
- **اجرا و گزارش‌دهی**: Assignee(های) تسک — دقیقاً مثل تسک پروژه‌ای (تیک‌زدن چک‌لیست، ثبت Task Report مجاز؛ ویرایش کلی/حذف غیرمجاز).

### چرخه‌ی وضعیت (Status Lifecycle) تسک فردی
```
todo → in_progress → done
         ↓
     cancelled (از هر وضعیتی ممکن است، توسط مدیر)
```
تغییر `status` توسط Assignee مجاز است (برخلاف عنوان/توضیحات که فقط مدیر ویرایش می‌کند) — چون در نبود بورد کانبان، این تنها راهی است که خود کارمند پیشرفت کارش را نشان دهد. این یک استثنا آگاهانه نسبت به قانون کلی "فقط مدیر ویرایش می‌کند" است و باید در کد به‌صراحت مجاز شود (نه اینکه به‌اشتباه Assignee بتواند بقیه‌ی فیلدها را هم عوض کند).

### چک‌لیست و Task Report روی تسک فردی
بدون تغییر — دقیقاً همان جداول و منطق سند Rotello (بخش‌های ۸ و ۹) چون این دو جدول اصلاً به `project_id` وابسته نیستند، فقط به `task_id`.

### API Endpoints مرتبط
| Method | Route | توضیح |
|---|---|---|
| `POST` | `/api/individual-tasks` | ساخت تسک فردی (فقط Owner) — بدنه بدون `project_id`/`column_id`، به‌جایش `assignee_ids` اجباری |
| `PATCH` | `/api/tasks/:id/status` | تغییر وضعیت (فقط برای تسک‌های فردی معتبر است؛ برای تسک پروژه‌ای باید از جابجایی ستون استفاده شود) |
| `GET` | `/api/individual-tasks?assignee=&status=` | لیست تسک‌های فردی با فیلتر |

> نکته: `POST /api/tasks` عمومی (سند Rotello) و `POST /api/individual-tasks` می‌توانند در سطح کد به یک Handler مشترک با شاخه‌بندی برسند، اما در سطح مستند API جدا نگه داشته شدند چون بدنه‌ی درخواست (Payload) واقعاً متفاوت است (یکی project_id/column_id می‌خواهد، دیگری نه).

### Edge Case ها
- **تبدیل تسک فردی به تسک پروژه‌ای (یا برعکس) بعد از ساخت؟** پیشنهاد: **مجاز نیست** در این فاز. اگر مدیر تصمیم گرفت یک تسک فردی باید وارد یک پروژه شود، باید تسک فردی را ببندد (`cancelled`) و یک تسک تازه در پروژه بسازد. تبدیل زنده یعنی مدیریت پیچیده‌ی `CHECK Constraint` بالا و مهاجرت وضعیت به ستون — ارزش پیچیدگی‌اش را در این فاز ندارد.
- **تسک فردی چندنفره و مسئولیت مبهم**: چون تسک فردی می‌تواند چند Assignee داشته باشد (جدول `task_assignees` محدودیتی نمی‌گذارد)، اگر مدیر بخواهد رفتار "دقیقاً یک نفر" را اجبار کند باید در Validation لایه‌ی Application این محدودیت (حداکثر یک Assignee برای تسک فردی) اضافه شود — این یک **تصمیم محصول باز** است (بخش ۵، سؤال ۱).

---

## ۲. دکمه‌ی تسک جدید (Quick Add) در صفحه‌ی اول داشبورد

### هدف
مدیر بدون نیاز به وارد شدن به بورد یک پروژه‌ی خاص، مستقیماً از صفحه‌ی اول داشبورد یک تسک جدید بسازد که خودش را در ستون "برای انجام" همان پروژه قرار می‌دهد.

### مشکل فنی که باید حل شود: "ستون برای انجام" یعنی کدام ستون؟
چون نام و تعداد ستون‌های هر پروژه Customizable است (بخش ۴ سند Rotello)، سیستم نمی‌تواند فرض کند "همیشه اولین ستون = برای انجام". دقیقاً همان مشکلی که با `is_done_column` حل شد، اینجا هم تکرار می‌شود.

### راه‌حل: افزودن فلگ متقارن به `board_columns`
```sql
ALTER TABLE board_columns ADD COLUMN is_entry_column BOOLEAN NOT NULL DEFAULT FALSE;
```
- وقتی یک پروژه‌ی جدید ساخته می‌شود (بخش ۴ سند Rotello)، اولین ستون پیش‌فرض (مثلاً "برای انجام") باید با `is_entry_column = true` ساخته شود.
- هر پروژه باید دقیقاً یک ستون با `is_entry_column = true` داشته باشد (قانون سطح Application؛ اعتبارسنجی هنگام ساخت/ویرایش ستون‌ها — اگر مدیر بخواهد ستون Entry را حذف کند، باید سیستم اجبار کند یک ستون دیگر را به‌عنوان Entry جدید تعیین کند، وگرنه این دکمه‌ی Quick Add برای آن پروژه از کار می‌افتد).

### فلوی کارکرد پاپ‌آپ
```
1. کاربر (مدیر) روی دکمه "تسک جدید" در صفحه‌ی اول داشبورد کلیک می‌کند
2. پاپ‌آپ باز می‌شود: همان فرم مودال ساخت تسک (بخش ۶ سند Rotello) + یک فیلد اضافه‌ی اجباری در بالای فرم: "انتخاب پروژه" (Dropdown از پروژه‌های غیرآرشیوشده‌ای که کاربر در آن‌ها Manager/Owner است)
3. بعد از انتخاب پروژه و تکمیل فیلدها (عنوان اجباری، بقیه اختیاری) → کلیک "ایجاد"
4. Backend:
   a. یافتن ستون Entry همان پروژه (WHERE project_id = X AND is_entry_column = true)
   b. محاسبه‌ی position (آخرین position آن ستون + فاصله‌ی استاندارد - همان منطق بخش ۵ سند Rotello)
   c. INSERT تسک با column_id یافت‌شده
   d. ثبت در task_activity_log با action_type='created'
5. نمایش پیام تأیید: "تسک به پروژه [نام پروژه] در ستون [نام ستون Entry] اضافه شد" + لینک مستقیم به آن تسک روی بورد (برای رفتن سریع در صورت نیاز)
```

### آیا این دکمه باید امکان ساخت تسک فردی را هم بدهد؟
با توجه به این‌که هر دو (تسک فردی و Quick Add پروژه‌ای) از یک نقطه‌ی ورود منطقی می‌آیند ("می‌خوام سریع یک کار جدید تعریف کنم")، پیشنهاد UX این است که همین پاپ‌آپ یک Toggle در بالا داشته باشد: **"تسک پروژه‌ای" / "تسک فردی"** — با انتخاب هرکدام، فیلد "انتخاب پروژه" یا فیلد "انتخاب Assignee" نمایش داده می‌شود. این باعث می‌شود کاربر فقط یک دکمه و یک پاپ‌آپ در ذهن داشته باشد، نه دو ورودی جدا برای مفهوم مشابه "تسک جدید". **این یک پیشنهاد طراحی تعاملی است، نه صرفاً دیزاین بصری** — روی منطق Backend اثری ندارد چون هرکدام endpoint خودش را صدا می‌زند (`POST /api/tasks` در برابر `POST /api/individual-tasks`)، فقط UI تصمیم می‌گیرد کدام را صدا بزند.

### دسترسی به این دکمه
فقط برای کاربرانی نمایش داده شود که حداقل در یک پروژه نقش Manager دارند یا Owner سراسری هستند — عضو عادی که در هیچ پروژه‌ای Manager نیست، اگر این دکمه را ببیند ولی هیچ پروژه‌ای در Dropdown نداشته باشد، تجربه‌ی بدی خواهد بود.

---

## ۳. پنل گزارش‌گیری فردی یکپارچه (Unified Individual Report)

### هدف
یک صفحه‌ی واحد در ادمین‌پنل که مدیر یک کارمند خاص و یک بازه‌ی زمانی را انتخاب می‌کند و **همه‌ی داده‌ی مربوط به آن کارمند از هر دو ماژول** را یک‌جا می‌بیند — بدون رفتن به دو بخش جدا. این پنل مستقیماً به سؤال "این کارمند این مدت چیکار کرده؟" جواب می‌دهد.

### چرا این پنل با `reporting module` قبلی (سند دوم) فرق دارد؟
سند دوم (`rokad-staff-reporting-module-brief.md`) روی **آمار کلی سازمان** تمرکز داشت (نرخ تکمیل کل تیم، روند کلی). این پنل جدید برعکس است: **عمیق و مختص یک نفر**، ولی از **هر دو ماژول** (هم گزارش روزانه‌ی تلگرامی، هم تسک‌های Rotello) داده می‌گیرد. این دو مکمل هم‌اند، جایگزین هم نیستند.

### ساختار داده‌ی خروجی این پنل (سه بخش مجزا)

**بخش الف — خلاصه‌ی وضعیت تسک‌ها (Snapshot لحظه‌ای، مستقل از بازه‌ی زمانی)**
این بخش "الان" را نشان می‌دهد، نه گذشته:
```sql
-- تسک‌های باز فعلی (فردی + پروژه‌ای)
SELECT t.*, p.name as project_name FROM tasks t
LEFT JOIN projects p ON p.id = t.project_id
LEFT JOIN board_columns bc ON bc.id = t.column_id
JOIN task_assignees ta ON ta.task_id = t.id
WHERE ta.employee_id = :employee_id
  AND t.is_deleted = FALSE
  AND (
        (t.project_id IS NOT NULL AND bc.is_done_column = FALSE)
        OR
        (t.project_id IS NULL AND t.status IN ('todo','in_progress'))
      )
```
خروجی: تعداد کل تسک باقیمانده، تفکیک بر اساس اولویت، تفکیک بر اساس عقب‌افتاده/به‌موقع.

**بخش ب — فعالیت در بازه‌ی زمانی انتخاب‌شده (Rotello)**
- تسک‌هایی که در این بازه تکمیل شده‌اند (`completed_at BETWEEN from AND to`)
- تمام `task_reports` نوشته‌شده توسط این کارمند در این بازه (تایم‌لاین متنی فعالیت‌هایش)
- تعداد آیتم‌های چک‌لیست که در این بازه توسط او تیک خورده‌اند (`checklist_items.done_by = employee_id AND done_at BETWEEN from AND to`)

**بخش ج — گزارش‌های روزانه (Rokad Staff)**
- تمام `daily_reports` این کارمند در بازه + `report_items` هرکدام
- نرخ به‌موقع بودن ارسال (چند درصد `on_time` بوده)
- روزهایی که اصلاً گزارش نداده (`missing`) در این بازه

### API Endpoint
```
GET /api/reporting/employee/:id/unified?from=&to=
```
پاسخ به‌صورت سه بلوک جدا (`current_tasks_snapshot`, `rotello_activity`, `daily_reports_activity`) برگردانده می‌شود تا فرانت‌اند بتواند هرکدام را جدا Render/Loading کند (چون منابع داده‌شان جدا و سرعت پاسخشان می‌تواند متفاوت باشد).

### Export
دقیقاً طبق زیرساخت Export موجود (بخش ۹ سند دوم، `exceljs`) — این پنل باید بتواند یک فایل Excel **چند شیت** (Multi-sheet) تولید کند: یک شیت برای تسک‌ها، یک شیت برای گزارش‌های روزانه — نه یک شیت درهم که خواندنش گیج‌کننده باشد.

### کنترل دسترسی
همان قانون Scope دپارتمانی سند دوم (بخش ۷) اینجا هم باید اعمال شود: اگر کاربر جاری نقش Supervisor دارد، فقط بتواند این پنل را برای کارمندان دپارتمان خودش باز کند، نه هر کارمندی با تغییر `id` در URL.

### چرا Pre-aggregation لازم نیست؟
چون این پنل **per-request برای یک نفر** اجرا می‌شود (نه برای کل سازمان هم‌زمان)، حجم داده‌ی هر Query محدود به یک کارمند در یک بازه است — سنگینی محاسباتی که باعث ساخت `daily_stats` در سند دوم شد، اینجا مصداق ندارد. Live Query با Index های موجود (`idx_daily_reports_employee_date`, `idx_task_assignees_employee`) کافی است.

---

## ۴. جمع‌بندی تغییرات Schema این سند

```sql
-- روی جدول tasks (موجود در Rotello)
ALTER TABLE tasks ALTER COLUMN project_id DROP NOT NULL;
ALTER TABLE tasks ALTER COLUMN column_id DROP NOT NULL;
ALTER TABLE tasks ADD COLUMN status TEXT;
ALTER TABLE tasks ADD CONSTRAINT chk_task_type CHECK (...);  -- بخش ۱

-- روی جدول board_columns (موجود در Rotello)
ALTER TABLE board_columns ADD COLUMN is_entry_column BOOLEAN NOT NULL DEFAULT FALSE;  -- بخش ۲
```

هیچ جدول جدیدی در این سند لازم نیست — همه‌چیز توسعه‌ی جداول موجود است.

---

## ۵. سؤالات باز این سند

1. **آیا تسک فردی باید محدود به یک Assignee باشد؟** جدول فعلی محدودیتی ندارد (بخش ۱، Edge Case اول). اگر پاسخ "بله فقط یک نفر" باشد، باید یک Validation صریح در `POST /api/individual-tasks` اضافه شود که آرایه‌ی `assignee_ids` را به طول ۱ محدود کند.
2. **آیا Quick Add (بخش ۲) باید برای پروژه‌های آرشیوشده هم در Dropdown نمایش داده شود؟** پیشنهاد پیش‌فرض: خیر — منطقی نیست تسک جدید به پروژه‌ای که بسته شده اضافه شود، ولی این تصمیم را باید تأیید کنی.
3. **در پنل گزارش‌گیری فردی (بخش ۳)، آیا Export باید شامل تسک‌های حذف‌شده (`is_deleted=true`) هم بشود** (برای شفافیت کامل تاریخچه) یا این‌ها همیشه از هر گزارشی مستثنی بمانند؟